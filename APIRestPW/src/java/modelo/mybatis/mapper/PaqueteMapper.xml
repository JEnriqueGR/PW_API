<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="paquete">
    
    <insert id="registrar" parameterType="pojo.Paquete">
        INSERT INTO paquete (idEnvio, descripcion, peso, alto, ancho, profundidad)
        VALUES (#{idEnvio}, #{descripcion}, #{peso}, #{alto}, #{ancho}, #{profundidad})
    </insert>
    
    <update id="editar" parameterType="pojo.Paquete">
        UPDATE paquete
        SET descripcion = #{descripcion}, peso = #{peso}, alto = #{alto},
            ancho = #{ancho}, profundidad = #{profundidad}
        WHERE idPaquete = #{idPaquete}
    </update>
    
    <select id="consultar-por-envio" parameterType="int" resultType="pojo.Paquete">
        SELECT idPaquete, idEnvio, descripcion, peso, alto, ancho, profundidad
        FROM paquete
        WHERE idEnvio = #{idEnvio}
    </select>
    
    <delete id="eliminar" parameterType="int">
        DELETE FROM paquete
        WHERE idPaquete = #{idPaquete}
    </delete>
    
    <select id="contar-por-envio" parameterType="int" resultType="int">
        SELECT COUNT(*)
        FROM paquete
        WHERE idEnvio = #{idEnvio}
    </select>
    
    <select id="obtener-por-id" parameterType="int" resultType="pojo.Paquete">
        SELECT idPaquete, idEnvio, descripcion, peso, alto, ancho, profundidad
        FROM paquete
        WHERE idPaquete = #{idPaquete}
    </select>
    
    <select id="obtener-todos" resultType="pojo.Paquete">
        SELECT idPaquete, idEnvio, descripcion, peso, alto, ancho, profundidad
        FROM paquete
    </select>
</mapper>
